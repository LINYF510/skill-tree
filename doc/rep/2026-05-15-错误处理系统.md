# 2026-05-15-错误处理系统

## 实现概述

为 Skill-Tree 项目实现了从数据层到 UI 层的完整错误处理体系，解决了 5 个核心问题：!! 非空断言、Repository 无错误捕获、ViewModel 错误处理重复、UI 错误展示混乱、Flow .catch 处理不一致。

### 错误分层模型

```
UI Layer          Snackbar / Dialog（通过 ErrorStateManager）
                  ↑  UiError（映射后）
ViewModel Layer   ErrorStateManager + handleDomainException / handleFlowException
                  ↑  Outcome.Error / DataException
Domain Layer      Outcome<T> / DomainException（业务规则错误）
                  ↑  (UseCase 层转换)
Data Layer        safeDbCall / .catch（Repository 异常捕获）
                  ↑  DataException（数据库/IO 错误）
Platform          SQLiteException / IOException
```

## 新增/修改文件列表

| 文件 | 操作 | 说明 |
|------|------|------|
| `core/data/exception/DataException.kt` | 新增 | 数据层异常基类（DatabaseError/FileNotFound/StorageFull/IoError） |
| `core/data/repository/SafeDbCall.kt` | 新增 | Repository 安全包装方法，捕获 SQLiteException/IOException |
| `core/ui/error/UiError.kt` | 新增 | UI 层错误模型（UiError/ErrorSeverity/ErrorAction/SnackbarEvent/DialogEvent） |
| `core/ui/error/ErrorStateManager.kt` | 新增 | 全局错误状态管理器，Snackbar/Dialog 事件流，DomainException/DataException 映射 |
| `core/ui/error/ViewModelErrorExt.kt` | 新增 | ViewModel 扩展函数 executeWithErrorHandling |
| `core/data/repository/SkillTreeRepositoryImpl.kt` | 修改 | 所有 suspend 方法用 safeDbCall 包装，Flow 方法添加 .catch |
| `feature/tree/SkillTreeViewModel.kt` | 修改 | 注入 ErrorStateManager，统一错误处理 |
| `feature/tree/SkillTreeScreen.kt` | 修改 | 消除 draggedNodeId!! 非空断言 |
| `feature/node/NodeDetailScreen.kt` | 修改 | 消除 5 处 uiState.node!! 和 currentViewingAttachmentPath!! |
| `MainActivity.kt` | 修改 | 注入 ErrorStateManager，集成 SnackbarHost 和错误 AlertDialog |

## 关键代码片段

### DataException 数据层异常

```kotlin
sealed class DataException(message: String, cause: Throwable? = null) : Exception(message, cause) {
    class DatabaseError(cause: Throwable) : DataException("数据库操作失败", cause)
    class FileNotFound(path: String) : DataException("文件未找到: $path")
    class StorageFull(requiredBytes: Long) : DataException("存储空间不足，需要 ${requiredBytes / 1024 / 1024}MB")
    class IoError(cause: Throwable) : DataException("文件读写失败", cause)
}
```

### safeDbCall Repository 安全包装

```kotlin
suspend fun <T> safeDbCall(block: suspend () -> T): Result<T> {
    return try {
        Result.success(block())
    } catch (e: android.database.sqlite.SQLiteException) {
        Result.failure(DataException.DatabaseError(e))
    } catch (e: java.io.IOException) {
        Result.failure(DataException.IoError(e))
    } catch (e: Exception) {
        Result.failure(DataException.DatabaseError(e))
    }
}
```

### Repository 改造模式

```kotlin
// suspend 方法：safeDbCall + getOrThrow/getOrNull
override suspend fun insertNode(node: SkillNodeEntity) {
    safeDbCall { skillNodeDao.insertNode(node) }.getOrThrow()
}

// Flow 方法：.catch 转换异常
override fun getAllNodes(): Flow<List<SkillNodeEntity>> {
    return skillNodeDao.getAllNodes()
        .catch { e -> throw if (e is DataException) e else DataException.DatabaseError(e) }
}
```

### ErrorStateManager 错误映射

```kotlin
fun mapDomainException(e: DomainException): UiError {
    return when (e) {
        is DomainException.CircularReference -> UiError("无法移动节点", "不能将节点移动到其子节点下，这会形成循环引用", severity = ErrorSeverity.WARNING)
        is DomainException.StorageError -> UiError("存储错误", "数据保存失败，请检查设备存储空间", severity = ErrorSeverity.ERROR)
        // ... 其他 10 种 DomainException 映射
    }
}

fun mapDataException(e: DataException): UiError {
    return when (e) {
        is DataException.DatabaseError -> UiError("数据库错误", "数据库操作失败，请重试", ErrorAction("重试") { }, ErrorSeverity.ERROR)
        is DataException.StorageFull -> UiError("存储空间不足", e.message ?: "存储空间不足", severity = ErrorSeverity.CRITICAL)
        // ... 其他 DataException 映射
    }
}
```

### ViewModel 统一错误处理

```kotlin
// DomainException 处理
private fun handleDomainException(e: DomainException) {
    val uiError = errorStateManager.mapDomainException(e)
    when (uiError.severity) {
        ErrorSeverity.CRITICAL -> errorStateManager.showErrorDialog(uiError.title, uiError.message)
        else -> errorStateManager.showSnackbar(uiError.message, isError = true)
    }
}

// Flow 异常处理
private fun handleFlowException(e: Throwable): String {
    return when (e) {
        is DataException -> {
            val uiError = errorStateManager.mapDataException(e)
            when (uiError.severity) {
                ErrorSeverity.CRITICAL -> errorStateManager.showErrorDialog(uiError.title, uiError.message)
                else -> errorStateManager.showSnackbar(uiError.message, isError = true)
            }
            uiError.message
        }
        else -> {
            errorStateManager.showSnackbar(e.message ?: "未知错误", isError = true)
            e.message ?: "未知错误"
        }
    }
}
```

### MainActivity Snackbar/Dialog 集成

```kotlin
LaunchedEffect(Unit) {
    errorStateManager.snackbarEvents.collect { event ->
        val result = snackbarHostState.showSnackbar(
            message = event.message,
            actionLabel = event.actionLabel,
            duration = if (event.isError) SnackbarDuration.Long else SnackbarDuration.Short
        )
        if (result == SnackbarResult.ActionPerformed) {
            event.action?.invoke()
        }
    }
}

// 错误对话框
dialogEvent?.let { event ->
    AlertDialog(
        onDismissRequest = { dialogEvent = null },
        title = { Text(event.title, color = TextPrimary) },
        text = { Text(event.message, color = TextSecondary) },
        confirmButton = { TextButton(onClick = { event.onConfirm?.invoke(); dialogEvent = null }) { Text(event.confirmLabel) } },
        containerColor = SurfaceDark
    )
}
```

### !! 非空断言消除模式

```kotlin
// 模式1：局部变量 + null 检查
val draggedId = draggedNodeId
if (draggedId != null) {
    viewModel.moveNode(draggedId, targetParentId)
}

// 模式2：局部变量 + smart cast
val node = uiState.node
if (node != null) {
    NodeTypeBadge(nodeType = node.nodeType)  // 替代 uiState.node!!.nodeType
    ContentSection(content = node.content)   // 替代 uiState.node!!.content
}
```

## 已知问题

1. Lint 报告的 CAMERA 权限缺少 `<uses-feature>` 标签是已有问题，非本次引入
2. `Icons.Filled.ArrowBack` 已弃用警告是已有代码，非本次引入
3. 其他 ViewModel（NodeDetailViewModel、StatisticsViewModel、SettingsViewModel）尚未改造使用 ErrorStateManager，后续可逐步迁移
4. `ErrorAction` 中的 `action` 回调目前是空实现（如"重试"按钮），需要在调用方传入具体重试逻辑
5. `executeWithErrorHandling` 扩展函数已创建但尚未在 ViewModel 中使用，可作为后续统一改造的入口
